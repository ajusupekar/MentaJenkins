pipeline {
        agent any
        stages
        {
            stage('Run functional test cases') 
            {
                steps 
                {              
                    bat "test verify -DjenkinsBrowser=${params.browser} -Dcucumber.options=\"--tags ${params.tagName}\""
                }
            }
      	}    
      }